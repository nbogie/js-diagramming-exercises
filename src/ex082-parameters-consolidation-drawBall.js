function drawBall(colour, x, y) {
    const diameter = 100;
    fill(colour);
    circle(200, 300, diameter);
}

const hitPoints = 80;
drawBall("green", hitPoints, 50);
console.log(diameter);

//Q1: What will be drawn on screen?
//    Where? What size?
//Q2: What will be printed to console?
